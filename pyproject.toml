[project]
name = "PCartLoader"
version = "1.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyudev>=0.24.4",
    "PySide6>=6.11.0",
]

[project.scripts]
PCartLoader = "src.main:main"

[tool.setuptools]
packages = ["src", "src.pcart"]

[tool.setuptools.package-data]
src = ["*.ui", "*.png"]

[tool.pyproject-appimage]
script = "PCartLoader"
output = "PCartLoader.AppImage"
icon = "./src/icon.png"
desktop-entry = "PCartLoader.desktop"
updateinformation = "gh-releases-zsync|carlgoshert|PCartLoader|latest|*.AppImage.zsync"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
